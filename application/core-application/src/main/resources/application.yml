spring:
  application:
    name: image-chronicle-application
  config:
    import:
      - security/application-postgresql.yml
    activate:
      on-profile:
        - default
        - local
        - dev
        - prod